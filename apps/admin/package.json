{
	"name": "admin",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "docker start admin-directus-1",
		"reset": "docker compose down && docker compose up -d",
		"log": "docker logs admin-directus-1 -f"
	},
	"dependencies": {},
	"devDependencies": {}
}
