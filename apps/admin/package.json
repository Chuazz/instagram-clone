{
	"name": "admin",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"reset": "docker compose down && docker compose up -d",
		"log": "docker logs admin-directus-1 -f"
	},
	"dependencies": {},
	"devDependencies": {
		"@biomejs/biome": "1.8.3"
	}
}
